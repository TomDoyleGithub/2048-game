const script = require('./assets/js/script.js');

describe("script", () => {
    describe("Adding block on start", () => {
        it("Should return two random html elements when activated"), () => {
            
        }
    })
})